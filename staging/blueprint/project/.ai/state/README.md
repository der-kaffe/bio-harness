# Private structured state

Create state files only when a deterministic consumer, ownership rule, and lifecycle are defined. Prefer concise, inspectable formats. Do not store secrets, duplicate Git, or treat generated state as authority.
